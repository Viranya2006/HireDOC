services:
  mongodb:
    image: mongo:7
    container_name: hiredoc-mongodb
    restart: unless-stopped
    ports:
      - "27017:27017"
    volumes:
      - hiredoc_mongo_data:/data/db
    environment:
      MONGO_INITDB_DATABASE: hiredoc

volumes:
  hiredoc_mongo_data:
